import { styled } from "@mui/material/styles";

export const HighlightPostContainer = styled("div")(({ theme }) => ({
  border: "solid 0.4px black",
  borderRadius: "4px",
  width: "64%",
  position: "relative",
}));
